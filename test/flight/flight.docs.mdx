---
labels: ['flight', 'entity']
description: 'A Flight entity.'
---

A component for a Flight entity, supports serialization and de-serialization of Flight objects and can be used from
both backend and frontend.

## Get started

Install and use the entity to create a new instance:

```ts
const flight = Flight.from({
  name: 'hello'
});
```
