---
labels: ['customer', 'entity']
description: 'A Customer entity.'
---

A component for a Customer entity, supports serialization and de-serialization of Customer objects and can be used from
both backend and frontend.

## Get started

Install and use the entity to create a new instance:

```ts
const customer = Customer.from({
  name: 'hello'
});
```
