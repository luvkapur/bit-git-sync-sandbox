---
description: 'A crm-ui app.'
labels: ['app', 'react']
---

A React application for crm-ui. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use crm-ui 
```

To run the app locally, run the following:

```
bit run crm-ui 
```
