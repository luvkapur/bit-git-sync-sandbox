---
description: 'A sky-ui app.'
labels: ['app', 'react']
---

A React application for sky-ui. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use sky-ui 
```

To run the app locally, run the following:

```
bit run sky-ui 
```
