/**
 * Index file for the Spinner component.
 * This file serves as the public API for the spinner, exporting the main component
 * and its associated props type.
 */

// Export the Spinner component implementation.
// The '.js' extension is used for local module imports as per project conventions.
export { Spinner } from './spinner.js';

// Export the SpinnerProps type definition for TypeScript users.
// The '.js' extension is used for local module imports as per project conventions.
export type { SpinnerProps } from './spinner.js';