export { syncProbe } from './sync-probe.js';
export const beat2 = 'this-line-was-born-in-git';

// diverged, git side: committed to the branch while the lane moved too.
export const divergedGitSide = 'git-moved-while-lane-moved';
